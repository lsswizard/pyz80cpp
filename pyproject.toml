[build-system]
requires = ["scikit-build-core>=0.8"]
build-backend = "scikit_build_core.build"

[project]
name = "PyZ80"
version = "2.0.0"
requires-python = ">=3.12"
dependencies = []

[tool.scikit-build]
cmake.source-dir = "."
